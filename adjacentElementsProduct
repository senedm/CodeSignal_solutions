# it finds the pair of adjacent elements that has the largest product and return that product.

def adjacentElementsProduct(inputArray):

        product =  [inputArray[i] * inputArray[int(i)+1] for i in range(len(inputArray)-1)]
        
        return(max(product))
